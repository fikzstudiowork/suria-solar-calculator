{
  "name": "suria-solar-calculator-root",
  "private": true,
  "scripts": {
    "dev:api": "node scripts/dev-api.mjs",
    "dev:web": "npm run dev --prefix frontend",
    "dev": "npx concurrently -n api,web -c blue,green \"npm run dev:api\" \"npm run dev:web\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
